If you want to train your model, you can refer to the code in example.py, or you can use other libraries to train your model.  
The model can be trained by annotating extracted features, then reading them in and converting them into arrays. 
